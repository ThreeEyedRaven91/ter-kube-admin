import dashboard from './dashboard';
import deployment from './deployment';
import namespace from './namespace';
import pod from './pod';

const API = {
  dashboard,
  deployment,
  namespace,
  pod,
};

export default API;
